///@desc blink(c_white, c_black, 0.5);       
///@arg on_val       
///@arg off_val       
///@arg [rate]   

function blink(on_val, off_val, rate_val)
{
	//on_val = argument[0];
	//off_val = argument[1];
	//rate = (argument_count > 2 ? (argument[2] * 1000) : 1000);
      
	//if (current_time % (rate * 2) >= rate) return on_val;
	//return off_val;
}